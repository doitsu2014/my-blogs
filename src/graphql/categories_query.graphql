query Categories {
  categories {
    nodes {
      id
      displayName
      slug
      categoryType
      categoryTags {
        nodes {
          tags {
            id
            name
            slug
          }
        }
      }
    }
  }
}
